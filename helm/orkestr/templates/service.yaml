apiVersion: v1
kind: Service
metadata:
  name: {{ include "orkestr.fullname" . }}
  labels:
    {{- include "orkestr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orkestr.selectorLabels" . | nindent 4 }}
