{{- if .Values.mariadb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "orkestr.mariadb.fullname" . }}
  labels:
    {{- include "orkestr.mariadb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: mysql
      protocol: TCP
      name: mysql
  selector:
    {{- include "orkestr.mariadb.selectorLabels" . | nindent 4 }}
{{- end }}
